# BluBridge Website — PRD

## Original Problem Statement
Complete visual and structural redesign of the BluBridge website combining approved sections from three reference deployments. STRICT CONTENT LOCK: no adding, removing, or paraphrasing existing text. Premium, minimal, light-themed, enterprise-grade editorial appearance.

Contact-page iterations: retain eleven isolated variants (`/get-in-touch` through `/get-in-touch-10`) with established BluBridge styling. `/get-in-touch-6` has required Budget currency (₹ / $ / €) + range dropdown between City and Message. Latest approved request: recreate the user's screenshot at `/get-in-touch-10` while keeping source `/get-in-touch-8` completely unchanged; rename admin Get in Touch labels to **AI Consultation Enquiry**; connect the new form so ALL submitted fields, selected AI services and consent choices are persisted and visible in admin details and CSV. Preserve current required/optional fields. Approved budget ranges remain Under 10,000; 10,000–50,000; 50,000–100,000; 100,000–500,000; 500,000+ in the selected currency.

Earlier approved scope (2026-09-21, since superseded by single-page forms below): `/ai-consulting` preserved existing pages and BluBridge branding with **Your Details → Project Details → Review**. User requested REAL Submit enquiry saving every field in **AI Consulting Enquiry** admin + CSV, with exact success text **Success Fully Submitted**. The old Project Enquiries admin page/menu/card was removed, its URL redirected to current AI Consulting Enquiry, and old records/public form behavior preserved.

Current approved scope (2026-09-22): six isolated AI consulting pages (`/ai-consulting` through `-5`). Base and variants1–3 retain one description that expands only under Other. Variants4 and5 separately received explicit approval for an independent required textarea under EVERY selected service, preserving multi-select, drafts on deselect/reselect, selected-only submission, and separate persisted responses. Variant4 was copied from3; variant5 was copied from the base. Keep their distinct layouts and all unrelated fields/header/footer unchanged. Never propagate a single-variant update through shared UI files.

## Architecture
- React frontend served via Express (`/app/frontend/server.js` serves `/app/frontend/build`). Current server includes a debounced source watcher that automatically runs `yarn build`; a manual `yarn build` can also generate the static bundle. Regular source changes do not require a supervisor restart.
- FastAPI backend (`/app/backend/server.py`, monolithic)
- MongoDB
- Security modules: `security_gateway.py`, `security_sessions.py`, `security_uploads.py`, `security_logging.py`. Admin uses opaque server-side sessions in HttpOnly cookies with CSRF and revocation (not browser-stored bearer tokens). Public POSTs use `frontend/src/security/formTransport.js` and `/api/form-context` proof tokens. Preserve these protections.
- V11 consultation request/response models: `/app/backend/consultation_models.py`; saved alongside legacy enquiries in `contact_enquiries`, no destructive migration.
- Global custom cursor: `/app/frontend/src/components/CustomCursor.jsx`

## Key Routes
- `/` Home, `/about-us`, `/careers`, `/research`, `/contact`, `/solutions/*`, `/products/*`
- Admin: `/admin`; old `admin` / `admin` is revoked. Retrieve current credentials privately per `/app/memory/test_credentials.md`; never put credential values in this document.
- `/get-in-touch-10` → `GetInTouchV11.jsx`, isolated CSS/components under `pages/get-in-touch-v11/`; real submissions.
- `/ai-consulting` → `AiConsulting.jsx`, isolated `.aic-page` styles and shared form components under `pages/ai-consulting/`; now a single-page form with REAL submission (supersedes historical wizard flow below).
- `/ai-consulting-1` → `AiConsultingVariant.jsx`, sticky left intro/right form; `/ai-consulting-2` → `AiConsultingWorksheet.jsx`, editorial worksheet.
- `/ai-consulting-3` → `AiConsultingDuplicate.jsx`, independent page composition and intro copy, reusing unchanged form components/hooks and the variant stylesheet; any extra styles are scoped to `.aic-third-page`.
- `/ai-consulting-4` → `AiConsultingVariant4.jsx`, copied from3 with its own intro/actions, `.aic-fourth-page` overrides, service section, validation adapter, form state and submission transport under `pages/ai-consulting-4/`. Shared base field components/styles remain read-only.
- `/ai-consulting-5` → `AiConsultingVariant5.jsx`, copied from the base centered-intro form, with its own intro text and full CSS snapshot scoped to `.aic-fifth-page`, including route-scoped country portals. Its per-service fields, validation adapter, controller and transport are local under `pages/ai-consulting-5/`.
- `/admin/get-in-touch` → **AI Consulting Enquiry**, with all contact/project fields in list/detail/search/CSV. Old `/admin/project-enquiries` redirects here; its menu/card/page are removed from active UI while stored records and backend APIs remain intact.
- Enquiry detail popup: `pages/admin/ConsultationDetail.jsx` with isolated `ConsultationDetail.css` and display-only `consultationDetailFields.js`; grouped data, fixed header/footer and independently scrolling body. Shared Dialog, data hooks and other routes are unchanged.

## Key API Endpoints
- `POST /api/contacts/submit`
- `POST /api/job-applications/submit`
- `POST /api/ai-consultation-enquiries` (v11 real form, public POST only, 201 on saved enquiry)
- `POST /api/ai-consulting-enquiries` (wizard-specific strict schema in `backend/consulting_wizard_models.py`, public POST only, saves to contact_enquiries, 201 receipt)
- `GET /api/admin/submissions/get-in-touch`, `GET/DELETE /api/admin/submission/{id}?form_type=get_in_touch`, `GET /api/admin/export/get_in_touch`, `GET /api/admin/export/all`

## Implemented (as of June 2026)
- All major pages visually overhauled per reference URLs
- Careers: hero decorations removed, purple → navy blue, orbit SVG removed
- Home: "Explore Solutions" button moved up 30px
- About Us hero: `.au-hero-side-col` padding-top set to 0 (desktop + mobile) — text aligns with group photo top
- **2026-06 (this session): Removed PUB/00x index and arXiv ID labels from all publication cards on Research page (kept Published date + View on arXiv button)**
- **2026-06 (this session): Home hero heading changed to "Building Tailored AI Systems / for Hard Problems" (was "Beyond the Horizon"), font clamp reduced to fit; hero subtitle changed to "a Frontier AI Research company", rebuilt + verified on preview**
- **2026-06-06 (this session): Verified About Us padding fix is live in PREVIEW (computed padding-top: 0px, text Y == photo Y == 457 on desktop; mobile OK). User's reported recurrence was caused by viewing the STALE DEPLOYED build (`main.ce732460.css`); current build is `main.f4b22752.css`. RESOLUTION: user must REDEPLOY.**
- **2026-02 (this session): 3 UI polish fixes — (1) Footer: switched to `.bb-footer-grid` with `grid-template-columns: auto auto auto auto minmax(320px, 1fr)` and uniform `column-gap: clamp(32px, 5.5vw, 88px)` for even inter-column gutters. (2) Careers Let's Connect: LinkedIn display text changed to "linked/blubridge" (href unchanged), email `careers@blubridge.com` now `white-space: nowrap`, channels use flex+space-between so all 4 fit without truncation. (3) Home Hero: reduced inner grid `pt-16 lg:pt-20` → `pt-8 lg:pt-10` (~48px less top space on desktop).**
- **2026-02 (this session): Meta Titles updated across 15 routes via `server.js` SSR + `useDocumentTitle` client hooks; Privacy Policy & Terms of Use backgrounds changed to `#F1F2FA` with `#FFFFFF` containers.**
- **2026-02 (this session): (1) Home hero headline: inserted "real" between "for" and "Hard Problems"; `.bb-real-shine` class applies accent color (`--bb-accent` #2b4c8c) + a subtle L→R reflective shine (4.5s loop, ~810ms pass) via `-webkit-background-clip: text` gradient position animation; `prefers-reduced-motion` disables animation. (2) Header announcement strip global text updated to "Tailored AI Systems: Large Language Models & Domain Specific Models" (styling preserved). (3) About Us "How We Build" rotating phrase: replaced typing animation with `PrecisionScanReveal` component cycling 4 phrases ("It's our Numerical Fidelity." / "…Training Throughput" / "…Inference Latency" / "…Evaluation Benchmarks") using clip-path masked reveal + 1px calibration line + navy dot at end, cubic-bezier(0.22,1,0.36,1), 2800ms cycle, IntersectionObserver pause, reduced-motion static fallback, invisible sizer reserves space (zero layout shift).**
- **2026-02 (this session): About Us statement animation upgraded to **Precision Metric Swap**: split into stationary "It's our" prefix + animated technical term; term enters from 11px below with clip-path L→R reveal + underline draw + navy dot, holds ~2.5s, exits up 9px with underline retract; `pms-active` uses `display: inline-flex; width: max-content` so the 1px calibration underline (`.pms-underline` at `width: 100%`) snaps to the ACTUAL rendered phrase width per cycle (verified: 550/580/541/658px for the four phrases) while the outer `.pms-sizer` reserves the longest-phrase width for zero layout shift; `aria-hidden` on rotating parts + a single visually-hidden accessible label; IntersectionObserver pauses off-screen; `prefers-reduced-motion` renders "It's our Numerical Fidelity." static with matching-width underline.**
- **2026-02 (this session): Two targeted micro-updates. (1) Careers `roleCards[1]` bullet reworded from "Paid internship is converted to full-time in 3-6 months period." → "Paid internship is converted to full-time **offer** in 3-6 months period." (single-word insertion, styling untouched). (2) About Us Precision Metric Swap refined: removed the underline (`.pms-underline`) + navy dot (`.pms-dot`) elements from JSX and neutralized their CSS; added trailing period to all four TERMS ("Numerical Fidelity.", "Training Throughput.", "Inference Latency.", "Evaluation Benchmarks.") — periods render from the string in the same Geist weight so they visually match the existing "Numerical Fidelity." period; animated term color changed from `#A5A5A3` → `#2b4c8c`; clip-path reveal replaced with a pure translateY slide (28px in-from-below → hold ~2.5s → 28px slide-up-out) with opacity fade; "It's our" prefix remains fully static; loop cycles Numerical Fidelity → Training Throughput → Inference Latency → Evaluation Benchmarks → back. Sizer/wrap sizing preserved so there is zero layout shift; reduced-motion fallback shows the first phrase statically.**

## Backlog
### P0
- No known login/base-form functional blockers after the 2026-09-22 follow-up below. Outstanding security-policy limitation: preview ingress rewrites application `SameSite=Strict` cookies to `SameSite=None; Partitioned`. Strict end-to-end enforcement requires infrastructure investigation outside application source; do not claim full strict-cookie compliance or weaken CSRF/origin checks to work around it.
### P1
- Await user selection of final contact-page variant.
- User verification of real `/ai-consulting` submit, exact success message and complete fields in AI Consulting Enquiry admin.
- Backend integration for `/get-in-touch-7`, `-8`, `-9` ONLY when requested; submissions remain frontend-only MOCKED by design.
### P2
- Success toast for contact form (replace browser alert)
- Active section highlight in Solutions menu
- Shared editorial ProductPageLayout for 8 industry pages
- Open Graph previews
- Potential enhancement: dedicated currency/budget filters for admin enquiry triage (not yet requested).
### P3
- Refactor backend server.py into MVC structure
- Consolidate FLUX/BluTrain research pages into one dynamic component
- Blog/Press pages

## Critical Notes for Next Agent
- STRICT CONTENT LOCK — never invent text
- Frontend serves a STATIC BUILD: verify automatic rebuild completion after editing `src/`, or run `cd /app/frontend && yarn build`. No regular source-change restart needed.
- If user reports a "recurring" visual bug, FIRST check whether they're viewing the deployed site (compare CSS build hash) before re-fixing code
- User wants minimal credit consumption

## Integrations
- Resend (emails, needs user API key), Clicky Analytics, MongoDB

## Update (2026-06 fork): Hero "real" artwork V2
- Replaced Caveat Brush font + SVG underline "real" with the user-supplied brush artwork (transparent PNG extracted from screenshot, bg #f1f1f5 removed, tight crop 208x142, saved at /app/frontend/public/real-brush.png).
- New impl: .bb-real-slot (overflow-hidden inline-block, width 0 -> 1.55em + margin-right sync, 5.2s loop, cubic-bezier(0.7,0,0.3,1)), img.bb-real-img fixed width for left-to-right reveal. aria-hidden img + sr-only "real" for a11y. prefers-reduced-motion keeps artwork visible.
- Removed Caveat Google Font import (unused elsewhere). Files: Home.jsx (hero third row), index.css.

## Update (2026-06 fork): Home compact header + hero spacing (desktop >=1024px only)
- Header.jsx: nav gets .bb-nav-home-compact class when route is '/' and not scrolled -> height 68px (was 76). Scrolled 64px and all other pages/mobile unchanged.
- Home.jsx hero: inline paddings moved to .bb-hero-home class (mobile 48/96 unchanged; desktop pt 24 / pb 80). Hero grid lg:pt-10 -> lg:pt-4.
- Verified @1348x926: gap nav-bottom->headline 139->99px, hero lifted 48px (both columns), section height 751->687px, About Us nav 76px, mobile intact, no h-scroll.

## Update (2026-06 fork): Fixed 8-9px band under Home navbar
- Root cause: main padding-top 112px vs compact Home header ~103px -> pale strip.
- Fix: App.js route-scoped .bb-main-home class (7rem mobile, 103px >=1024px); Header.jsx border-bottom 1px solid #d4d8e8 on Home (transparent unscrolled elsewhere, unchanged).
- Verified by testing_agent (iteration_18.json, 100% pass): no band, 1px border, nav 68->64 on scroll, other pages + mobile untouched.

## Update — Jun 2026 (fork)
- Removed the 5.2s reveal/hide loop on the hero word "real" (deleted `bb-real-slot` keyframes/animation in index.css). The brush artwork is now permanently static in its fully revealed state (width 1.55em, margin-right 0.22em). No other hero element changed. Verified via screenshot: identical state at load and after 5.5s, animationName=none.
- PENDING (P0, recurring x12): full frontend regression test via testing_agent (CustomCursor pointer-events clickability check).

## Update — Jun 2026 (Solutions menu fix)
- FIXED: Solutions dropdown worked only once. Root causes: (1) Header.jsx un-cleared close timeouts snapped dropdown shut on re-hover — added openDropdown/closeDropdown with timer refs; (2) ScrollToTop.jsx only watched pathname so hash clicks on /solutions never scrolled — now depends on full location and scrollIntoView on hash (scroll-margin-top 120px for the 3 section ids).
- Verified by testing_agent (iteration_19.json, 35/36 pass). P0 site-wide regression ALSO done: CustomCursor not blocking clicks, nav/CTAs/contact form all clickable.
- New backlog item confirmed by test: contact form clears silently after submit — needs success toast (already P2).

## Update — Sep 2026 (Get in Touch — v1 rebuilt + 2 visual variants + admin)
- /get-in-touch (GetInTouch.jsx, .git-*): intro (GET IN TOUCH / "Let's build what's next." / lede), left continuous panel with EXACTLY 4 items (icon+title): Project Assistance. (3 bullets, CTA Explore solutions → /solutions), Business enquiries. (mailto info@blubridge.com), Data & privacy. (CTA Contact us → mailto:privacy@), Security reporting. (CTA Report an issue → mailto:support@, small note). Right white form: First Name*, Last Name*, Company Email* (+ client-side Verify Email, same pattern as /contact), Role*, Tell us about your project* (≤1000, counter), optional marketing checkbox, privacy text (→ /policies/privacy-policy), CTA "Talk with our team".
- /get-in-touch-1 (GetInTouchV2.jsx, .git2-*): SAME content/logic; premium navy split canvas (large headline hero, numbered 01–04 editorial left, navy form surface right, offset frame, dot grid).
- /get-in-touch-2 (GetInTouchV3.jsx, .git3-*): SAME content/logic; LIGHT theme only — wide hero, 4-column bordered module grid (2-col ≤1279, 1-col ≤639), full-width form below.
- /get-in-touch-3 (GetInTouchV4.jsx, reuses .git3-* + scoped .git4-page spacing overrides): identical to /get-in-touch-2 but section order INTRO → FORM → 4 CARDS (desktop + mobile). Self-verified: order, submit 200, verify, no overflow.
- /get-in-touch-4 (GetInTouchV5.jsx, scoped .git5-*): restrained corporate LIGHT redesign of /get-in-touch-3 — hero (headline 62% / lede 38%), form directly on page with 1px border (no shadow, 2px radius), readable 13px uppercase Inter labels, 50px inputs, joined email+verify component, medium-width navy CTA; 'HOW CAN WE HELP' + 4 columns split by thin vertical rules (no numbers), 2×2 ≤1279px, stacked rows with horizontal rules ≤639px. Same content/logic/backend. Self-verified: submit 200, verify, no overflow. FIX (iteration_22): removed max-width:880px on .git5-form-body so fields span the full form container (right-side gap bug).
- /get-in-touch-5 (GetInTouchV6.jsx, scoped .git6-*): two-column version of /get-in-touch-4 — hero, then form LEFT (63%) + 'HOW CAN WE HELP' 2×2 shared-border grid RIGHT (37%); tops aligned; stacks on ≤1023px, grid becomes ruled single column ≤639px. Same content/logic. Verified iteration_23.
- /get-in-touch-6 (GetInTouchV7.jsx, scoped .git7-*): minimal two-column light page — heading+lede LEFT (sticky), NEW form RIGHT with Slalom-style field set: Full name*, Email*, Phone*, Company*, Job title*, Country (select, optional), City*, Message (optional ≤1000), required privacy checkbox, optional marketing checkbox, 'Submit'. Flat fields with navy underline. NO 'How can we help' grid. NEW backend POST /api/project-enquiries → collection project_enquiries (full_name,email,phone,company,job_title,country,city,message,privacy_consent,marketing_consent,status,created_at,updated_at), 422 validation, 409 dedupe/email/min, email notify 'project_enquiry'. NOT yet in admin panel. Verified iteration_24 (14/14 backend + frontend).
- /get-in-touch-7 (GetInTouchV8.jsx, scoped .git8-*): FRONTEND-ONLY (no API/backend by user request — backend to be added later). Heading LEFT, placeholder-style form RIGHT: First Name*, Last Name* / Company Email*, Company Name* / Job Title*, Phone Number (optional) / Select a country* (full) / Comments* (≤300, counter 'N of 300 max characters') / Terms checkbox (→ /policies/terms-conditions, /policies/privacy-policy) / full-width 'Start a conversation'. Frontend validation only; valid submit shows notice 'Form validated successfully. Backend connection will be added later.' Self-verified: 8 inline errors on empty submit, counter, zero network calls, mobile stack.
- /get-in-touch-8 (GetInTouchV9.jsx, scoped .git9-*): FRONTEND-ONLY (no backend by request). Heading LEFT (44%), minimal form RIGHT (max 600px): Name, Email, Company's website (optional, URL-validated), Services select (8 BluBridge options), Project size select (5), details textarea, full-width 'Get in touch'. Visible labels, light theme, no card. Valid submit → notice 'Form is ready. Backend integration will be added later.' Self-verified: 5 errors on empty submit, URL error, zero network calls, mobile stack.
- /get-in-touch-9 (GetInTouchV10.jsx, scoped .git10-*): FRONTEND-ONLY. Reference-pattern: pale-blue form panel LEFT ('Let's Connect': First Name, Last Name, Phone, Organization Name, Business Email, Select Enquiry (same 5 options as /contact), 'Schedule a Meeting'); RIGHT: existing heading + direct contact from /contact (Phone +91 8925987250, Email info@blubridge.com, Location Besant Nagar Chennai office, LinkedIn). Valid submit → dev notice, no API. Self-verified.
- UPDATE: 'How can we help' 4-section block REMOVED from /get-in-touch-3 (V4) and /get-in-touch-4 (V5) per user — both pages are now hero + form only. Also restored `let time = 0;` in solutions/ValueRealization.jsx (someone had commented it out → lint blocker).
- Backend POST /api/contact-enquiries: body {firstName,lastName,email,role,message,marketingConsent}; stores id, first_name, last_name, company_email, role, project_details, marketing_consent, status=new, created_at, updated_at (no extra columns). 422 blank/invalid/>1000, 409 dup per email/min. Email notification via send_contact_form_email("get_in_touch").
- Admin: sidebar "Get in Touch" → /admin/get-in-touch (GetInTouchForms.jsx): list/search/pagination, view (marks viewed), delete, CSV export. Endpoints: GET /api/admin/submissions/get-in-touch, /api/admin/submission/{id}?form_type=get_in_touch (GET/DELETE), /api/admin/export/get_in_touch, stats.get_in_touch in /api/admin/dashboard/stats. Dashboard stat card + quick link.
- Tested: iteration_21.json — backend 22/22 (tests/test_get_in_touch_full.py), frontend 3 routes + admin + /contact regression pass. Tester's "409 UX drift on /get-in-touch-2" was a false positive (429 rate limit) — self-verified 200→409 friendly message.
- NOTE: frontend is a static build with an automatic rebuild watcher; allow ~25s or run `cd /app/frontend && yarn build`. Do NOT use `// eslint-disable-line react-hooks/exhaustive-deps` (rule not registered → build fails).

## Update — Sep 2026 (Resume storage migration)
- Migrated job-application resume uploads from pod-local disk (uploads/resumes) to Emergent Object Storage (blubridge/resumes/{uuid}.ext). Added init_storage/put_object/get_object helpers + startup init; EMERGENT_LLM_KEY added to backend/.env.
- /api/admin/resume/{id} now serves from object storage with legacy local-file fallback for pre-migration submissions.
- Verified end-to-end: submit -> storage upload -> admin download bytes match. Fixes blocking lint [ephemeral-upload-storage].

## Update — 2026-09-19: Required project budget + admin integration
- `/get-in-touch-6` → `GetInTouchV7.jsx`: required Budget row between City and Message, default ₹, selectable ₹/$/€, five approved ranges, inline validation, scoped `.git7-budget-controls` styling, reset to ₹/empty range after successful submission.
- POST `/api/project-enquiries`: required validated `budget` string, e.g. `€ 100,000–500,000`; all 15 combinations accepted, missing/null/blank/unsupported values rejected with 422. Persists to MongoDB `project_enquiries` and includes budget in the existing email notification payload. Existing records need no migration; output models allow absent budget.
- IMPORTANT correction to handoff: ProjectEnquiries admin page did NOT exist at session start. Implemented `/admin/project-enquiries` with sidebar/dashboard integration, searchable paginated list, budget column, detail modal with all fields, viewed status, confirmed deletion, refresh, CSV export. Older records show `Not provided` in list and details.
- Backend routes: GET `/api/admin/submissions/project-enquiries`; GET/DELETE `/api/admin/submission/{id}?form_type=project_enquiry`; GET `/api/admin/export/project_enquiry`; combined `/api/admin/export/all` includes project records and Budget. Dashboard stats include project_enquiries. Existing admin token verification reused; no credentials or authentication behavior changed. MongoDB `_id` excluded; typed project list/record/receipt models used.
- Frontend files added: `pages/admin/ProjectEnquiries.jsx`, `ProjectEnquiryDetail.jsx`, `useProjectEnquiries.js`. Added optional closeTestId support to shared Dialog, admin login input/button test selectors and linked labels. AdminLayout gets min-w-0 for wrapping.
- Smoke test found the closed mobile nav panel translated beyond viewport. Header now hides the closed panel; opening/closing/link navigation verified. Other contact variants' content and styles unchanged. Cursor ring/dot already had pointer-events:none; actual clicks now verified.
- Verification: build compiled; Python compile passed; iteration_25 backend **21/21 passed**, including all valid budgets, invalid required values, persistence, protected admin CRUD/export/stats. Frontend real submission/reset/currency-switch, admin actions, navigation and desktop/mobile verified. Final self-test resolved the tester's only coverage gap: a temporary legacy record with long unbroken name/company text rendered `Not provided` in list/detail; no overflow at 1920×800 or 390×844, and test fixture deleted. See `/app/test_reports/iteration_25.json` and `iteration_25_followup.md`.
- Existing Resend send calls logged success during tests; inbox receipt not independently checked. No new integrations or credentials. `/get-in-touch-7`, `-8`, `-9` remain **MOCKED** frontend-only as requested.
- Next action: user review at `/get-in-touch-6` and `/admin/project-enquiries`. No known blockers in this scope; P1/P2/P3 backlog above remains deferred.

## Update — 2026-09-21: Screenshot duplicate /get-in-touch-10
- Created independent `GetInTouchV11.jsx` route with `get-in-touch-v11/EnquiryFields.jsx`, `useEnquiryForm.js`, and `GetInTouchV11.css`. Light screenshot-matched layout: centred intro; six multi-select AI services; Your details / Your project columns; privacy/update checkboxes and CTA. Generative AI selected initially; Budget defaults to Not sure yet.
- Kept global site header/footer as explicitly approved. **Source `/get-in-touch-8` unchanged**: SHA-256 checks for GetInTouchV9.jsx, index.css, Header.jsx, Footer.jsx all pass; no source styles edited. All new styling is .git11-page scoped.
- Initial frontend-only version passed iteration_26 except one minor mobile subtitle spacing issue. Fixed with explicit whitespace around the responsive line break; verified in main screenshots and iteration_27. Initial MOCKED behaviour was subsequently replaced by real submission at user's explicit request below.

## Update — 2026-09-21: AI Consultation Enquiry naming + real backend flow
- Exact admin name **AI Consultation Enquiry** applied to sidebar (desktop/mobile), page heading, dashboard card/quick link and related description. Public wording and existing admin route `/admin/get-in-touch` unchanged.
- User clarified to retain current requirements: full name, work email, company, at least one AI service and privacy consent required; phone/job title/country/city/budget/description optional; marketing consent optional.
- New public POST `/api/ai-consultation-enquiries` validates and saves every field in MongoDB. Request fields: fullName, workEmail, company, phone, jobTitle, country, city, budget, description, services, privacy, marketing. Model forbids extra keys, enforces allowed services and budgets, validates optional phone, strips whitespace and lowercases email; duplicate same-email submissions within 60 seconds receive 409. Only the exact new POST is added to the public allowlist; existing admin authentication unchanged.
- Storage mappings: `full_name`, `company_email`, `company`, `phone`, `role` (job title), `country`, `city`, `budget`, `project_details` (description), `services`, `privacy_consent`, `marketing_consent`, `source=/get-in-touch-10`, `status`, UTC `created_at/updated_at`. Existing first_name/last_name records remain intact and readable. Typed response models exclude BSON _id.
- Real frontend save: sends one request, disables controls while submitting, prevents double clicks, resets only after success, shows a thank-you. 409/422/429/server/network errors show actionable feedback and preserve inputs. No authentication or API credentials changed. Existing notification helper logs successful sends; inbox receipt not independently verified.
- Admin page split into small components: GetInTouchForms.jsx, useConsultationEnquiries.js, ConsultationTable.jsx, ConsultationDetail.jsx. List shows name/email/company/role/services/budget/date/status; details show ALL form values plus both consents/source/time. Supports search, pagination, refresh, viewed status and confirmed delete. New and legacy records supported.
- Dedicated and combined CSV exports include every new field and both consent flags; dangerous spreadsheet prefixes escaped. Dedicated download name now ai_consultation_enquiries_*.csv.
- Testing: production build + Python compile passed. `/app/test_reports/iteration_27.json`: **21/21 backend tests**, frontend success/reset/error preservation, admin labels/CRUD/search/pagination/export, source isolation, legacy compatibility and responsive 1920×800 / 390×844 flows passed. Main screenshots also show no overflow and fixed mobile subtitle. No unresolved defects.
- Test cleanup: corrected over-escaped QA email patterns in the tester's cleanup script, then removed eight remaining temporary consultation records and two QA project regression records. Non-QA records untouched.
- Remaining mocks: ONLY `/get-in-touch-7`, `/get-in-touch-8`, `/get-in-touch-9` remain frontend-only **MOCKED** by prior request. `/get-in-touch-10` is now REAL.
- Next: user validation of a submitted enquiry under Admin → AI Consultation Enquiry. P1 final-variant selection and P2 admin budget/service filters remain optional backlog, not implemented.

## Update — 2026-09-21: /ai-consulting Project Details wizard
- Added standalone `/ai-consulting` route and `AiConsulting.jsx`; retained the same page intro, Geist/IBM Plex Mono fonts, light BluBridge palette, header/footer, and 970px container. Only new page files and App.js routing changed. All existing V11 page/components/CSS, Header/Footer and index.css pass original SHA-256 checks (`/tmp/ai-consulting-protected-before.sha256`).
- Approved flow: Your Details (existing full name/work email/company/phone/job title/country/city + consent fields) → Project Details → read-only Review. Back/Edit preserve local entries. Review shows `Not submitted`; final Submit enquiry is disabled. No form API calls, persistence, or backend changes.
- Project Details uses exact user title/subtitle and ten service labels from `pages/ai-consulting/options.js`. Native checkbox cards are two columns on desktop, one column below 1024px; keyboard/focus states and selected blue treatment. Guidance is mutually exclusive with all normal options, including Other. Other shows a required specification input; removing it clears its hidden value/error.
- Required description textarea, project stage and expected timeline with all exact placeholders/options. Radio budget type switches exact Project/Monthly USD labels and options immediately; resets prior estimated budget even if `Budget not yet defined` exists in both sets. Estimated budget starts disabled until type selected. Required budget status includes all five user-supplied statuses. Pairs stack on tablet/mobile.
- Inline validation blocks Review & Continue until all seven required project fields and conditional Other are valid; whitespace rejected, first invalid control focused, errors clear as values become valid. Red stars and subtle errors, accessible native controls and focus styles; no intrusive alerts, decorative graphics or distracting animation.
- Modular files: `options.js`, `validation.js`, `useConsultingWizard.js`, `FormField.jsx`, `ContactDetails.jsx`, `ServiceSelection.jsx`, `ProjectDetails.jsx`, `ReviewDetails.jsx`, `AiConsulting.css` under `frontend/src/pages/ai-consulting/`.
- Verification: `yarn build` successful. Main smoke desktop1920×800/mobile390×844 overflow arrays empty. `/app/test_reports/iteration_28.json` reports all requested checks passed: exact options/order, exclusive guidance, Other, radio/reset logic, required/whitespace validation, keyboard interactions, Back/Edit preservation, read-only Review, zero mutating API calls and no new storage. Additional 320/768/1024/1440 responsive checks passed. No unresolved defects.
- **MOCKED/non-submitting:** `/ai-consulting`, plus earlier `/get-in-touch-7`, `-8`, `-9`. `/get-in-touch-10` remains REAL and untouched. No credentials created or changed.
- Next action: user review of the new wizard. Backend/admin integration is intentionally deferred; potential future enhancement is a downloadable enquiry summary.

## Update — 2026-09-21: Wizard real submission, success message, old admin removal
- User reported Submit enquiry disabled, then explicitly approved backend saving and all data in **AI Consulting Enquiry**. This supersedes the earlier frontend-only scope. Enabled real final submission, while Continue/Review & Continue remain navigation only.
- New `consulting_wizard_models.py` validates all wizard fields, exact service/stage/timeline/budget/status options, exclusive guidance, conditional Other, privacy and contact values. POST `/api/ai-consulting-enquiries` added to exact public POST allowlist; existing admin authorization remains unchanged.
- Saves in `contact_enquiries`: prior full_name/company_email/company/phone/role/country/city/services/project_details/consents plus **other_requirement, project_stage, start_timeline, budget_type, budget, budget_status**, source `/ai-consulting`, status and UTC timestamps. Extended optional output fields preserve all legacy/V11 records. Notification is attempted after saving; email failure cannot undo the save.
- New `useWizardSubmission.js`: sending latch prevents double clicks, pending state disables edit/back/submit, HTTP/network errors retain data, server field errors return to the appropriate step, recent duplicate email receives409. On201 shows exact **Success Fully Submitted** plus explanatory confirmation/reference, locks submitted review, and offers Start a new enquiry.
- Admin name is now exact **AI Consulting Enquiry** (sidebar/page/dashboard/detail); route remains `/admin/get-in-touch`. Detail shows all new wizard fields, conditional specification and dynamic USD project/monthly budget label. List distinguishes project/monthly budgets. Dedicated/combined CSV exports append all five new project fields; both consent flags and every previous field retained.
- User requested only old Project Enquiries UI removal. Removed its sidebar/menu and dashboard card, removed active page import/mount, and changed `/admin/project-enquiries` to a replace redirect to `/admin/get-in-touch`. Existing `project_enquiries` records and old public `/get-in-touch-6` API flow preserved; no destructive deletion/migration. Legacy unused page source remains unmounted.
- Validation: Python compile and frontend build passed. Main live submit reproduced user's Other/Test/project-budget screenshot and returned201. Iteration_29 **21/21 backend tests passed**, full admin data/CSV, old records preservation, old-route redirect/menu removal, success/retry/reset/double-click and desktop/mobile verification passed. Tester found Enter in pre-review text fields advanced the wizard; fixed by preventing implicit Enter from INPUT elements on early steps while preserving keyboard activation of Continue buttons and textarea line breaks.
- Final targeted browser test: Enter on step1 and step2 inputs stays on step with zeroPOSTs; explicit keyboard Continue works; Enter on Review Submit sends exactly one real POST; exact success wording shown. Overflow offenders [] at1920×800 and390×844. Temporary keyboard test record deleted through authenticated API. See `/app/test_reports/iteration_29_followup.md`.
- Protected V11 page/components/styles, Header/Footer/index.css hashes unchanged. No credentials changed. Only earlier `/get-in-touch-7`, `-8`, `-9` remain **MOCKED**; `/ai-consulting` and `/get-in-touch-10` are REAL.
- Next: user review; optional future follow-up statuses or downloadable enquiry summary. No unresolved issues in this scope.

## Update — 2026-09-21: Isolated /ai-consulting-3 screenshot copy
- Approved scope: change the third variant's intro using the supplied new-copy screenshot and existing intro screenshot; preserve `/ai-consulting-1`, all form content/behaviour, backend, and the established light layout.
- Replaced the thin wrapper around `AiConsultingVariant` with independent page JSX in `AiConsultingDuplicate.jsx`. Shared field components, validation/submission hooks, introCopy.js and source stylesheet remain untouched. The existing App.js route needed no change.
- New left intro, transcribed from reference `eer6o7u1_image.png`: **AI CONSULTING SERVICES** / **Turn Ambition Into Measurable Outcomes.** / **BluBridge combines frontier AI research with full-stack engineering to build tailored AI systems. From strategy and custom model development to deployment and support, we work around your business goals.**
- Updated only this route's client meta description to match its paragraph. No dark redesign, extra marketing CTA, benefit blocks, or form text changes. Added `.aic-third-page`-only headline wrapping and a narrow-desktop font adjustment; no shared styles modified.
- Verification: production build compiled; all 26 protected-source SHA-256 checks passed. Live browser checks at 1920×800 and 390×844 passed exact copy, unchanged form text vs variant 1, required validation, clearing errors after input, default +1/SVG flag, and variant 1 content after route switching. Horizontal overflow arrays were empty at both sizes. See `/app/test_reports/ai_consulting_3_copy_verification.md`.
- No real enquiry submitted during this copy-only check; backend unchanged and no new mocks or credentials. Existing `/get-in-touch-7`, `-8`, `-9` submissions remain **MOCKED** by prior request.
- P0: none remaining for this request. P1/Next: user review of `/ai-consulting-3` and final variant selection. P2: other form integrations and site improvements remain deferred. Optional enhancement: compare enquiry completion rates across variants before choosing the final version.

## Update — 2026-09-21: Restore marked CTA and benefits on /ai-consulting-3
- User clarified the marked screenshot content was missing, then approved adding the complete section while retaining the existing light design. This supersedes the prior omission of CTA/benefit blocks.
- Added isolated `pages/ai-consulting-3/IntroActions.jsx`: **Talk to Our AI Experts** button with arrow, followed by three icon benefits: **Strategy to execution**, **Real business impact**, **A partner for what’s next**. All added interactive and content elements have unique test IDs.
- CTA is non-submitting; scrolls to the form below the fixed header and focuses the first enabled input without resetting existing values. Reduced motion uses immediate native scrolling. Short desktop viewports disable only the third variant's sticky intro to keep the expanded content accessible. The other variants, shared styles/components, validation, backend and submission API are untouched.
- Verification: build passed, desktop 1920×800/mobile 390×844 screenshots visually inspected with overflow arrays empty. `/app/test_reports/iteration_32.json`: 100% targeted frontend pass; mouse/Enter/Space activation preserves input, no submit side effects, all exact benefit labels/icons present, responsive DOM checks passed at 320/768/1024/1440, original variant isolation and required validation passed. All 26 protected-source hashes remain unchanged. No fixtures or credentials created; no live enquiry submitted or APIs mocked.
- Tester suggested `auto` as an optional scrolling compatibility alternative; no failing behaviour was reported. Retained native `instant` to guarantee no motion for reduced-motion users (supported by the current browser ScrollBehavior API).
- P0: none outstanding. Next/P1: user visual review of the now-complete marked section. Backlog unchanged; `/get-in-touch-7`, `-8`, `-9` remain **MOCKED**. Optional future improvement: measure CTA-to-enquiry completion to evaluate the final variant.

## Update — 2026-09-21: One conditional Other textarea across all four AI consulting URLs
- Exact approved scope: retain all service choices and every other form field on `/ai-consulting/`, `/ai-consulting-1/`, `/ai-consulting-2/`, `/ai-consulting-3/`; remove the duplicate Other specification input. Latest clarification: **Tell us about your requirement** is hidden initially, expands inside **Other - Please specify** only when selected, and collapses on deselect. It is required only while Other is selected.
- Shared `RequirementsSection`/`ServiceSelection` render exactly one 5000-character textarea inside selected Other; no standalone description or `otherRequirement` input remains. Checkbox/label and native keyboard Space toggle expansion. Draft text survives toggle/reselect, but is omitted from meaningful submission data (`requirement=''`) when Other is unselected; hidden validation errors clear immediately.
- Frontend validation and `AIConsultingWizardEnquiry.requirement` enforce the same conditional requirement, including whitespace rejection for Other. Non-Other submissions accept omitted/empty descriptions. Optional legacy `otherRequirement` is still accepted/preserved/exported for old clients/records but is no longer required. New enquiry admin details omit the empty legacy Other row; older saved details remain accessible. `server.py`, storage mappings and auth were not changed.
- Verification: frontend build/Python compile passed. Main browser verified initial collapsed, click/unclick, draft preservation and conditional required errors across all four exact URLs. Desktop1920×800/mobile390×844 screenshots show the expanded section and empty overflow arrays. An earlier resize check flagged offscreen hidden cursor coordinates; a fresh mobile load with pointer inside viewport confirmed no layout overflow, without any cursor/global CSS changes.
- `/app/test_reports/iteration_33.json`: **7/7 backend/persistence tests passed**, all scoped frontend checks passed. Real UI submissions with Other and without Other succeeded with exact success message; inspected payloads and admin APIs confirm correct saved `project_details`, no duplicate field, and no hidden-draft leak. Blank/whitespace Other rejected; description longer than1000 accepted; no-Other omitted/empty accepted; legacy CSV compatibility verified. Responsive320/390/768/1024/1440/1920 checks passed. CTA/benefits on variant3 remain unchanged.
- Tests added: `backend/tests/test_ai_consulting_other_requirement_v34.py` and one-shot live-UI record verification `test_ai_consulting_ui_live_records_iter34.py` (its temporary fixture IDs were cleaned up after this run). No production code changes by tester. All temporary QA entries cleaned; no credentials created/modified. No scoped flows/APIs mocked.
- P0: none. P1/Next: user review of the collapsed/expanded behaviour on all four pages. P2/P3 backlog unchanged; existing `/get-in-touch-7`, `-8`, `-9` are still **MOCKED** and deferred. Potential enhancement: optional enquiry draft recovery.

## Update — 2026-09-22: Sticky complete intro on /ai-consulting-3 desktop
- User requested the marked left panel remain fixed while scrolling; approved scope is only variant3, desktop, below the navigation, bounded before the footer; mobile and other pages unchanged.
- Reproduced at1920×800: route-specific max-height850px rule set `.aic-alt-intro` to static, moving its top from180px to−420px after600px scroll. Removed that disabling rule.
- Only `pages/ai-consulting-3/AiConsultingDuplicate.css` changed: at widths>=1024px the full intro is sticky at124px, with viewport-bounded max-height and vertical overflow access when needed. Small inset padding preserves focus-outline room. It remains bounded by its original aside, so it releases before the footer. Mobile continues using existing static positioning. No copy, shared form, backend or other variant changes.
- Verification: build passed. Desktop live test with Other expanded and text entered: intro top remained124px at scroll450 and950; heading, paragraph, CTA and all benefits visible; CTA still scrolled/focused Full Name. Footer test found intro bottom302px before footer top374px. Mobile390×844: position static, intro moved by the exact900px page scroll; no pinned overlay. Horizontal overflow arrays empty at both widths. Screenshots visually inspected under `/root/.emergent/automation_output/20260922_080917/`.
- P0: none remaining; P1/Next: user review of desktop sticky behaviour. Backlog and existing MOCKED `/get-in-touch-7`–`-9` unchanged. No API changes/mocks, enquiries submitted, or credentials changed. Optional future enhancement remains enquiry draft recovery.

## Update — 2026-09-22: Remove Contact Permission heading on all four forms
- User approved removing only the visible **Contact Permission** heading on `/ai-consulting`, `/ai-consulting-1`, `/ai-consulting-2`, `/ai-consulting-3`.
- Replaced the permission section's heading-producing wrapper with a plain section retaining its existing class, `data-testid`, and accessible `aria-label`. Checkbox, consent wording, Privacy Policy link, required status, error display, section spacing/dividers and submit button remain unchanged. No shared styling, hooks, validation, API, or backend edits.
- Verification: build compiled; all four URLs passed at desktop1920×800 and mobile390×844. Heading/test selector absent, three remaining form headings intact, exact consent text/link verified, checkbox toggles, required consent error appears on invalid submit and clears when checked. No enquiry POSTs sent during invalid-form tests. All eight overflow arrays empty. Desktop/mobile screenshots inspected: `/root/.emergent/automation_output/20260922_082610/`.
- P0: none; Next/P1: user review. Existing backlog, including MOCKED `/get-in-touch-7`–`-9`, remains unchanged. No new credentials or mocks. Optional future enhancement: enquiry draft recovery.

## Update — 2026-09-22: Variant3 intro wording
- User explicitly confirmed replacing **BluBridge combines** with exact **We Combine** on `/ai-consulting-3` only. Changed the opening words of `enquiryDescription` in `AiConsultingDuplicate.jsx`; all remaining paragraph text, heading, layout, controls and other pages are unchanged. The existing meta-description hook uses the same updated text.
- Verification: build compiled; exact complete paragraph and client meta description confirmed in browser; desktop1920×800/mobile390×844 screenshots visually inspected, overflow arrays empty. Output: `/root/.emergent/automation_output/20260922_083113/`.
- No new credentials, integrations, API changes or mocks. P0:none; Next/P1:user review. Backlog unchanged, including existing MOCKED contact variants7–9. Optional enhancement remains enquiry draft recovery.

## Update — 2026-09-22: Independent variants4/5 and per-service requirement responses
- Earlier paused work: duplicated `/ai-consulting-3` to `-4`, preserving its We Combine intro, CTA/benefits, sticky desktop panel and static mobile layout. Source/copy text and dimensions matched in desktop/mobile smoke checks before service changes. User then explicitly requested per-service expansion only on4; implemented and tested its core UI/API (10 backend cases initially), with remaining admin/export verification subsequently completed in iteration34 below.
- Duplicated base `/ai-consulting` to `-5` (not variant3/4): identical centered **Discuss Your AI Requirement** intro, text, contact/timeline/budget/consent controls, shared backend and initial Other-only behaviour. Own component/copy and CSS namespace. Source/copy text and form dimensions matched at1920×800 and390×844; originals protected by source hashes. User then explicitly approved replacing only5's service-selection section with per-service expansion.
- **Current behaviour on4 and5:** each of the9 selected service checkboxes renders its own immediately attached textarea. Standard label **Tell us about your requirement**; placeholder **Briefly describe what you need help with regarding [exact display name].** Other uses **Please specify your requirement** / **Please describe the AI service or requirement you are looking for.** Multi-select preserved; unchecking collapses/removes only that field's required error and excludes its draft from submission, without clearing the draft or changing other selections.
- Empty/whitespace selected responses show exactly **Please tell us about your requirement.** beneath the specific textarea; focus goes to the first invalid input. Each string max5000. New4 textareas140px; new5 textareas150px. Existing blue focus, error styling, fonts, spacing/radii and service names preserved. Desktop two-column grid with each selected panel in its own column, Other spanning the existing full-width row; mobile one column, no overlap/overflow. Contact, timeline/budget, consent, intro, header/footer and other pages unchanged.
- Data/API: `serviceRequirements` is a selected-only dictionary using display names and `Other`; canonical services list retains legacy `GPU & AI Systems Optimisation`, mapping its dictionary key to **AI Performance Optimization**. Local controllers send `formVariant: ai-consulting-4` or `ai-consulting-5`. Backend schema explicitly accepts both, requires all selected responses (including when map omitted/null), rejects unselected keys/bad types/oversize values with JSON422 and nested service error locations. New transports map these back to individual textareas. No common requirement string is submitted for these variants.
- Persistence: existing `contact_enquiries` documents store `service_requirements` dictionary and `form_variant`; `source` intentionally remains `/ai-consulting` for legacy admin semantics. `project_details` remains empty for new per-service flows. Existing response model exposes the dictionary; no BSON identifiers returned. Legacy payloads without markers/maps retain original validation/storage. No destructive migration.
- Admin/exports: display each separate service/response in detail (not duplicated as a generic description); both enquiry and combined CSV add a **Service Requirements** JSON column preserving all responses. Existing notification payload creates a readable summary from the dictionary for the unchanged notification function; email delivery itself was not verified in these tests.
- **Verified:** build/Python compile passed. Main browser completed the requested final8 interaction checks on5 at1920×800/390×844 with screenshots `/root/.emergent/automation_output/20260922_104340/`; both overflow arrays empty. All36 protected source hashes passed after implementation/testing, including variants0–4, shared fields/styles/hooks, header/footer and existing server persistence/helper files.
- **Testing report `/app/test_reports/iteration_34.json`: 18/18 backend tests, all scoped frontend checks passed.** Includes real variant5 submission, one POST on double-click, selected-only payload, admin detail/mobile long-text rendering, complete JSON CSV exports, per-service missing/whitespace/unknown/oversize validation, variant4 and legacy regressions. Responsive widths320/390/768/1024/1440/1920 passed with all9 expanded and long unbroken content. Temporary QA records cleaned. No production edits by testing agent; no new/changed credentials or mocked APIs.
- Tests: `backend/tests/test_ai_consulting_variant4_service_requirements_v35.py`, `test_ai_consulting_variant4_admin_export_v35.py`, `test_ai_consulting_variant5_service_requirements_v35.py`; combined JUnit `test_reports/pytest/iteration_34_backend.xml`.
- P0:none remaining. Next/P1:user review of `/ai-consulting-5/`; preserve isolated variants for subsequent design edits. P2/P3 backlog unchanged, including MOCKED `/get-in-touch-7`–`-9`. Optional enhancement: enquiry draft recovery. Missing optional `auth_testing.md` is not a defect in this scope; existing `memory/test_credentials.md` is valid and unchanged.

## Historical checkpoint — 2026-09-22: Whole-site security audit and hardening
- User explicitly authorized a complete, backup-first security audit and surgical hardening of every page/form/API/admin/upload/database path, not only AI consulting. Must preserve content/layout/URLs/legitimate functions and data. Current connected environment only; stop before edits if restoration cannot be verified. No destructive tests on legitimate records.
- **Backup gate passed before hardening:** complete encrypted application snapshot restored byte-for-byte (hashes/modes/symlinks), source checkpoint `97fbaaa6b14dc53e0f927745580849ced26cc822` restored, full configured database restored into a separate random namespace and compared by BSON/counts/indexes/options. Live source database unchanged; only temporary verification namespace removed. Private backups `/root/blubridge-security-backups/20260922T140355Z/`; no secrets/data in public report. Initial provider database-name length issue fixed in private backup script, second restore succeeded. See `security/BACKUP_AND_ROLLBACK.md` for procedure/limits.
- Backups are currently environment-local, not offsite disaster recovery. Some résumé references already point to missing local files; no files/data deleted by this task and missing originals cannot be recovered from the available environment.
- Read-only security audit and main-agent inspection confirmed active default administrator credential (verified existing SHA-256 credential hash without printing it), permissive contact extra fields/client IDs, spoofable forwarding-header rate identity, weak hash/localStorage session handling, missing CSP, and additional GET-side-effect/path-handling concerns. See `security/AUDIT_PROGRESS.md`. No application hardening code or credential/data changes yet.
- Authentication integration playbook requested before any auth edits; returned bcrypt/HttpOnly-cookie guidance with a generic JWT template, requiring careful adaptation to existing `/api/admin/*` URLs and stricter CSRF/revocation requirements. No Google/public-registration feature to be added.
- P0: secure operator credential handover, remove default authentication path, strict public field allowlisting; then complete endpoint inventory, site-wide hardening and isolated security/regression tests. Task is **NOT COMPLETE**. Do not reuse the old weak administrator credential as a secure production/test credential after rotation; keep new secrets out of Git/chat and update credential documentation with private configuration retrieval instructions.

## Update — 2026-09-22: Secure admin access and base form verification
- **User approval:** finish base `/ai-consulting/` loading/double-submit verification, dropdown layering/focus and canonical punctuation only; keep variants1–5 and unrelated backlog unchanged. User then reported `Invalid credentials` with the revoked default and explicitly chose to **keep the secure password**, verify access and retrieve current credentials privately. No password/account/security configuration changed in this follow-up.
- **Admin result:** configured `ADMIN_USERNAME` / `ADMIN_PASSWORD` from restricted `backend/.env` work in both API and real browser. Dashboard loads, session survives reload, no auth token is stored in localStorage, mobile logout succeeds and subsequent verify returns401. Owner instructed to privately open the two keys in `backend/.env`; credentials remain absent from chat/source/reports.
- **Handoff corrections:** `iteration_36.json` did not exist at fork start; actual latest was35 and had incomplete browser verification rather than proven broken disabled/focus behavior. Existing `useWizardSubmission` already has a synchronous sending-ref latch, and `useEnquiryForm` already disables the whole form immediately. Real baseline keyboard/typeahead and focus checks passed. These working shared hooks were left untouched.
- **Production edits (3 base-only files):** `AiConsulting.jsx` adds a spinner, `aria-busy` and live loading label to existing disabled submit state; `ai-consulting-base/Dropdowns.css` sets both portaled menu types to1100 and explicitly scopes visible focus styling plus reduced-motion spinner handling; `ai-consulting-base/DropdownField.jsx` renders canonical en/em dashes locally. Shared `options.js` and its legacy display normalization are intentionally unchanged so other variants do not change. Existing Other display label, all option values/order, privacy wording and unbroken Privacy Policy reference remain intact.
- **Backend submission bug:** previously corrected `company_email` duplicate guard is verified with201 then409 and exactly one saved `contact_enquiries` row; no new backend application edits were needed.
- **Build/visual verification:** automatic build compiled. Desktop1920×800/mobile390×844 show correct punctuation, visible keyboard focus, dropdown hit-testing above page content, scrolling long options, usable consent and zero horizontal overflow. Screenshots: `/root/.emergent/automation_output/20260922_164648/`. All103 protected shared/other-variant source hashes remain unchanged.
- **Testing-agent report:** newly generated `/app/test_reports/iteration_36.json` records2/3 backend tests passing; remaining external Strict-cookie assertion fails. Agent browser proof was inconclusive. Do not interpret its unresolved dropdown reports as a confirmed user-facing defect; targeted follow-up below completed the actual flows without further dropdown code edits.
- **Real browser follow-up:** `/app/backend/tests/verify_base_browser_followup.py` verifies desktop keyboard and mobile pointer selection through all six dropdowns, project/monthly budget reset, conditional Other, consent, invalid empty submission with zeroPOST, immediate disabled/loading/spinner during a delayed **real** request, exactly onePOST on double-click plus Enter,201, exact success message, locked controls and one persisted row with canonical values. Both viewport runs passed and exact QA rows were deleted. No mocked API responses. Reports: `iteration_36_followup_form.json` (20 checks passed) and `iteration_36_followup_admin.json` (3 functional checks passed,1 infrastructure limitation). Earlier `iteration_36_followup.json` is a superseded partial run: analytics prevented `networkidle`, resolved in test code by waiting for DOM/control readiness.
- **Cookie-policy limitation (UNRESOLVED):** application cookie construction verified `Secure; HttpOnly; SameSite=Strict; Path=/`; external preview receives `SameSite=None; Partitioned`, independently confirmed by investigation and browser. This rewrite is outside `/app` source. Login, CSRF, strict origin checks and session revocation still operate; no insecure workaround implemented. External Strict assertion must remain visible as failing until infrastructure is corrected. Whole-site security has NOT been re-audited in this follow-up.
- **Next/P1:** owner sign-in and base-form visual review; infrastructure owner to investigate cookie attribute rewriting. P2/P3 remain deferred, including backend integration for **MOCKED** `/get-in-touch-7`, `-8`, `-9`, industry layouts, social previews and broader refactoring. Suggested future enhancement: optional enquiry draft recovery.

## Update — 2026-09-23: Owner-authorized administrator password replacement
- User requested username `admin` and a replacement password, then explicitly chose a generated strong password after being informed the initial11-character suggestion did not meet the12-character policy. Username was already `admin`; no account recreation or weaker password exception needed.
- Generated a new cryptographically random43-character password, rotated through existing authenticated/CSRF-protected `/api/admin/change-password`, and synchronized `ADMIN_USERNAME`/`ADMIN_PASSWORD` in restricted mode0600 `backend/.env`. All unrelated environment keys retained. No secret printed or placed in reports/source/chat; credential retrieval instructions updated in `memory/test_credentials.md`.
- Existing bcrypt hashing, minimum length, CSRF, strict origin checks, rate limits and session controls unchanged. Incremented account auth_version and revoked all prior sessions through the existing change-password behavior. No frontend or backend application source edits.
- **Verified:**18 recorded checks in `test_reports/admin_rotation_20260923.json`: authenticated rotation200, new hash matches, old password does not, old login401, prior-session replay401, all old sessions removed, new login/verify/dashboard200, logout200 then401. Restarted backend after `.env` change and reverified new credentials/dashboard/logout; test sessions all closed.
- **Next:** owner retrieves `ADMIN_PASSWORD` privately from `backend/.env` and signs in at `/admin`; saving it in a password manager is recommended. Prior preview SameSite rewrite limitation remains unresolved and unchanged. Other P1/P2/P3 work remains deferred, including **MOCKED** contact variants7–9. No integrations or new mocked flows added.

## Update — 2026-09-23: Readable AI Consulting Enquiry Details popup
- **Approved request:** user supplied a screenshot of `/admin/get-in-touch` with a narrow, ungrouped, whole-dialog scrollbar and asked to show it properly. Approved widening desktop, grouping contact/project/budget/consent details, improving spacing, pinning heading/close while scrolling, mobile stacking, and preserving all values/functionality.
- **Implemented:** popup now960px maximum on desktop and366px at390px mobile; viewport-bounded with8px corners. Five unframed sections: Contact information, Project requirements, Budget, Consent, Submission details. Two-column desktop grid and one-column mobile; full-width requirements preserve whitespace and wrap long strings. Higher-contrast readable labels/values, section dividers, white content and subtle footer band match existing BluBridge styling.
- Header/title and40px close target stay fixed; only the details body scrolls. Close/Delete footer and any deletion error remain visible. Keyboard-focusable body supports scrolling; existing Radix focus trap/Escape/dismiss behavior retained. Every new section/heading/control/value has a unique test ID, and existing field/action test IDs are preserved.
- **Isolation/data:** only existing `ConsultationDetail.jsx` changed, plus new local CSS and display-field grouping helper. All original field labels, values, conditions, missing-value fallbacks, consent semantics, name fallback, project/monthly budgets and per-service requirement display retained. No database/API/auth/CSV/public-variant changes; re-exported existing `consultationName` keeps the unchanged table import working. All208 protected source hashes matched.
- **Verified:** build compiled; actual screenshot-matching already-viewed enquiry opened with current secure credentials. Main browser checked all displayed key values, pinned header/footer/close, full body scroll and zero horizontal overflow at1920×800 and390×844; screenshots visually inspected in `test_reports/admin_detail_layout/`. One initial429 came from pre-existing login rate limits; waited for window expiry without changing security settings.
- **Testing report `test_reports/iteration_37.json`:16/16 passed**, including legacy missing fields/name fallback, monthly/Other/false-consent values, variant4/5 long/unbroken per-service responses, responsive layout, Escape/X/footerClose, focus trap/body keyboard scrolling, scroll reset on opening another record, confirmed/cancelled delete, busy-disabled delete, visible deletion error, viewed-marking and CSV inclusion. APIs real; negative error check used temporary test-only network abort (no production mocks). Four tagged fixtures removed by exact ID; main independently confirmed zero remain. No testing-agent production edits.
- **Next/P1:** user visual review of updated popup. Optional future enhancement: one-click copying of contact details. Existing preview-cookie SameSite limitation remains unchanged; unrelated P2/P3 backlog and **MOCKED** contact variants7–9 remain deferred. No credentials created or modified in this task.

## Update — 2026-09-24: Google Ads tag on public pages
- User supplied `AW-18460200148` and explicitly chose public pages only (not admin). Added server-rendered head injection in `frontend/googleAdsTag.js`, nonce assignment through existing `securityHeaders.js`, exact Google script/image/connection CSP origins from frontend environment, and no wildcard/unsafe script policy changes. Tag ID and URL configured with `GOOGLE_ADS_TAG_ID`/`GOOGLE_ADS_SCRIPT_URL`.
- `public/admin-route-boundary.js` runs before vendors and forces a fresh document when navigation crosses public/admin boundaries, ensuring admin CSP and removal of loaded analytics. Ordinary public SPA navigation remains unchanged. `/index.html` redirects to Home so it cannot bypass nonce/tag rendering.
- Verified one script/config in actual head; gtag.js and collection requests200; no Google CSP errors; direct admin and public-to-admin have no tag/config/global; admin-to-public restores tag; no duplicate initialization through all46 public SPA routes. No Ads account access or conversion label supplied, so no custom conversion event added. User interrupted this task to request publication filtering; verification completed with the route task below.

## Update — 2026-09-24: Approved public-page list and `/consulting` move
- **Final approved scope:** retain Home; About Us; Careers,4job pages and Join Our Team; existing Products/Solutions/industries/Research; `/contact` only; all3policies; existing protected admin. Move only the base AI consulting form to `/consulting`. User specifically excluded Media Kit, contact/consulting variants and all unlisted pages. Approval received before edits.
- **46 public URLs and8 existing admin routes** now listed in shared `frontend/src/routing/publishedPages.json`; readable exact list in `memory/LIVE_PAGES.md`. `pageAccess.js` enforces the catalog in React and Express. Removed inactive route/import wiring only; excluded page source files preserved. Replaced wildcard Home/products/solutions fallback with proper NotFound and direct HTTP404/noindex. Unknown jobslugs are also404. Existing admin authentication/API behavior untouched.
- `/ai-consulting` and its slash alias return permanent308 to `/consulting`, preserving query parameters; SPA redirect also preserves fragment. `/consulting/` remains valid. **The form component/hooks/styles/backend endpoint and stored source marker remain unchanged**, including stored `source='/ai-consulting'` for historical/admin compatibility. No existing data migrated or deleted.
- `frontend/pagePublishing.js` generates sitemap/robots from the exact catalog; obsolete static copies removed. New `PUBLIC_SITE_URL=https://blubridge.com` retains prior sitemap origin. Admin excluded from sitemap/disallowed in robots. Preview-infrastructure noindex behavior remains unchanged; this work controls page availability, not external deployment.
- Updated only active links that would break: Narvik overview -> `/contact`; Serverless and FineTuning GPU links -> `/products/gpu-nodes`. Layouts/text on retained pages unchanged. Existing header/footer already link only to retained pages. No new menu items added.
- Added route-specific consulting metadata; testing identified9 pre-existing Home metadata fallbacks, corrected through `publishedPageSeo.js` for `/products`,4jobs,BluTrain and3FLUX article routes. No research/job page content edits.
- **Testing:** original `iteration_38.json` had12/13 backend passes and incomplete browser submission proof; metadata fixed and targeted46-route retest passed (`pytest/published_seo_followup.xml`). Investigation attributed the recurring dropdown report to unsynchronized automation, not a reproduced form defect; proven main browser tests pass20 checks on desktop/mobile with real submissions, onePOST, oneDB record, success, consent and exact cleanup (`published_form_followup.json`).
- Full46-page browser crawl and rendered link validation passed after fixing remaining FineTuning link; Google single-init/admin isolation and desktop/mobile404 recovery pass (`published_navigation_followup.json`). Screenshots visually verified; no horizontal overflow on changed screens. Build compiled;51/51 protected backend/admin/base-form source hashes unchanged. Summary: `test_reports/iteration_38_followup.md`.
- **Next/P1:** user reviews `/consulting` and final URL inventory. No in-scope blockers. Prior infrastructure SameSite limitation remains; no credentials changed. Other P2/P3 work deferred. Previously **MOCKED** contact variants7–9 now return404 (source preserved). Optional next enhancement: configure an enquiry-success Google Ads conversion after user provides/approves the conversion label.

## Update — 2026-09-24: Products/Solutions restricted to main pages (CURRENT publication scope)
- User explicitly clarified restriction applies only to these2 sections: retain `/products` and `/solutions`; hide all product/solution/industry detail pages. Keep previously approved Home,Company,Careers/job details,JoinOurTeam,Research,Contact,Consulting,Policies and protectedAdmin unchanged. This supersedes the earlier46-public-page inventory.
- `publishedPages.json` now contains **22 public URLs** and the same8 admin routes. Removed24 detail imports/routes from App.js (9products,7solutions,8industries); source components remain on disk. Shared client/server page policy returns404 for direct and SPA access, including trailing/case aliases. Sitemap automatically reflects only22public URLs. `/ai-consulting`308redirect remains intact.
- No active internal links on retained pages led to the removed routes; no navigation rewrite needed. `/solutions#model-customization`, `#value-realization` and `#deployment` remain valid sections, not separate detail pages. Products cards' old link values are in inactive/commented rendering and do not expose detail routes.
- **Verification:**52 main HTTP assertions pass across22retained/24removed URLs plus aliases; sitemap/catalog/admin/other-group consistency passes (`section_visibility_http.json`). Testing-agent report39 confirms both main pages/CTA/tabs, SPA404+Home recovery, consulting field/dropdown/consent smoke and public-only Google tag work. No forms submitted or records modified in this task.
- **Minor existing readability findings fixed:** bounded Solutions decorative glow at right0 instead of-8%; Products H1 uses responsive36/48/60px sizes (desktop unchanged), and Products CTA heading nowwhite on darkbackground. No layout/content restructuring. Final main desktop1920×800/mobile390×844 screenshot verification confirms complete text and zero overflow (`iteration_39_followup.md`; screenshots `/root/.emergent/automation_output/20260924_112710/`).
-210/212 protected source files byte-identical; only documented `index.css` and `Products.jsx` readability edits differ. All backend/auth/admin/consulting code and archived detail components preserved. No credential changes or mocked live integrations.
- **Current page list:** `memory/LIVE_PAGES.md` updated to22 URLs. **Next:** user reviews the2 main pages. Other backlog remains deferred; optional enhancement is making Products a direct navigation link. Prior preview SameSite/noindex infrastructure behavior remains unchanged; previouslyMOCKED contact variants7–9 remainunpublished404.

## Update — 2026-09-24: Consulting title only
- User explicitly approved changing only `/consulting`'s HTML/browser title to **Consulting Services & Solutions | BluBridge**. Visible heading, meta description, form, routing and other pages must remain unchanged.
- Updated the one title string in `frontend/src/pages/AiConsulting.jsx` and the consulting SEO title in `frontend/server.js` (HTML ampersand encoded as `&amp;`). No other production edits in this request.
- Build compiled. HTTP checks passed for `/consulting`, `/consulting/`, and the308 legacy `/ai-consulting` redirect; exact decoded title and unchanged description verified (`test_reports/consulting_title_update.json`). Browser verified exact tab title, unchanged visible heading, independent Contact title and correct title when returning via Back. Final verification log/screenshot: `/root/.emergent/automation_output/20260924_113201/`.
- No unresolved title issues, credential/data changes or new mocked flows. Publication catalog remains22 public pages. **Next:** no required follow-up; optional future enhancement is matching social-share metadata. All other backlog remains deferred.
